// import { create } from 'zustand';

// Store for authentication state using Zustand
// export const useAuthStore = create((set) => ({
//   user: null,
//   setUser: (user) => set({ user }),
//   clearUser: () => set({ user: null }),
// }));

export const useAuthStore = () => {}; // Placeholder until zustand is installed
